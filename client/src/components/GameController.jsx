import "./GameController.css";

const GameController = ({
    board,
    gameStats,
    player,
    setGameOver,
    setPlayer
}) => {
    return (
        <input
            className="GameController"
            type="text"
        />
    );
}

export default GameController;